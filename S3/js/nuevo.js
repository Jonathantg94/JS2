// CREANDO FUCION FLECHA PARA QUITAR DUPLICADOS
const quitarDuplicados = (arr = undefined) =>{
    // validar si el arreglo es undefined
    if (arr === undefined ) return console.log("no ingresaste un arreglo");
}